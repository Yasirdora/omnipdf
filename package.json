{
  "name": "omnipdf",
  "private": true,
  "version": "0.0.0",
  "description": "OmniPDF monorepo — the universal structured-document PDF engine.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
